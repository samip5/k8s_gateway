apiVersion: v2
name: k8s-gateway
description: A Helm chart for the k8s_gateway CoreDNS plugin
type: application
version: 3.2.1
appVersion: 1.2.1
maintainers:
  - url: https://github.com/samip5
    name: samip5
annotations:
  artifacthub.io/changes: |
    - kind: fixed
      description: added default "Service" and "Ingress" to watchedResources
    - kind: fixed
      description: initialize gateway api controller only when required
